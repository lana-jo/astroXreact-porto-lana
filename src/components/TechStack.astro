---
import { techStacks } from '../data/techStacks';
---

<section id="techstack" class="fade-up card reveal delay-2">
	<div class="porto-header">
		<div>
			<h2>Tech Stack</h2>
			<span class="section-subtitle">Core Technologies & Frameworks</span>
		</div>
	</div>
	<div class="chips">
		{techStacks.map((stack) => (
			<span class="chip" title={stack.name}>
				<img
					src={`https://cdn.simpleicons.org/${stack.icon}/${stack.color}`}
					alt={`${stack.name} logo`}
					width="16"
					height="16"
					loading="lazy"
				/>
				{stack.name}
			</span>
		))}
	</div>
</section>

